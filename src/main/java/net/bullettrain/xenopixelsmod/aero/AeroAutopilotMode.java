package net.bullettrain.xenopixelsmod.aero;

/** Runtime flight-director selection. Autopilot engagement is deliberately not persisted. */
public enum AeroAutopilotMode {
    MANUAL,
    TARGET,
    ROUTE
}
