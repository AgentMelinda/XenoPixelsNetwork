package net.bullettrain.xenopixelsmod.effect;

import net.minecraft.world.effect.MobEffect;
import net.minecraft.world.effect.MobEffectCategory;

/** Display / marker effect (no vanilla attribute mods unless added later). */
public class XenoMobEffect extends MobEffect {
    public XenoMobEffect(MobEffectCategory category, int color) {
        super(category, color);
    }
}
