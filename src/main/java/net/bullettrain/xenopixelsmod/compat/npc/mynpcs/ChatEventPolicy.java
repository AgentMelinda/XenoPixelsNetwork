package net.bullettrain.xenopixelsmod.compat.npc.mynpcs;

/** Bounds mutable chat text exposed through the optional My NPCs script bridge. */
final class ChatEventPolicy {
    static final int MAX_MESSAGE_LENGTH = 256;

    private ChatEventPolicy() {
    }
}
