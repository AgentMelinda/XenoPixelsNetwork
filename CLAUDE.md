# Claude Repository Entry Point

Follow `AGENTS.md` as the canonical repository contract, then read `ai/README.md` and
`ai/agents/claude.md`. Historical `CLAUDE_*_HANDOFF*.md` files are dated context only.
