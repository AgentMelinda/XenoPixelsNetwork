# GitHub Copilot Instructions

Follow root `AGENTS.md` as the canonical repository contract. Use `ai/README.md`,
`ai/agents/copilot.md`, and the relevant file under `ai/skills/` for task-specific guidance.

Never invent APIs, packet identifiers, mixin targets, dependency versions, or resource paths.
Preserve existing working-tree changes and require focused validation before suggesting completion.
